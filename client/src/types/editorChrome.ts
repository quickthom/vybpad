/** INTERFACES.md — editor chrome enums shared by UIStore and settings UI. */
export type StaffSpacing = 'compact' | 'default' | 'wide';

export type EditorLabelMode = 'degree' | 'roman' | 'both' | 'off';
